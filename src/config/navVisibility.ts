/** Menú superior — TIENDA (/tienda). La ruta sigue activa si es false. */
export const SHOW_TIENDA_NAV = false;

/** Panel docente — Tabla de contenido en /docente-clases. Visible solo PC/.exe vía `isDesktopPcOrExe()`. */

/** Precios en tarjetas de cursos, tienda, eventos y planes. PayPal sigue activo si es false. */
export const SHOW_SECTION_PRICES = false;

/** Mi Mundo — botón LIVE en tarjeta de perfil (navega a /pc). */
export const SHOW_PROFILE_LIVE_BUTTON = false;

/** Mi Mundo — icono configurar stream (navega a /conciertos-live/config). */
export const SHOW_STREAM_CONFIG_BUTTON = false;

/** Red social inmersiva — botón «Ir a Salas» (/nuestras-salas). La ruta sigue activa si es false. */
export const SHOW_RED_SOCIAL_IR_SALAS_BUTTON = false;

/** Inicio — botón «Explorar» en tarjeta Tiendas inmersivas (/tienda). La ruta sigue activa si es false. */
export const SHOW_TIENDAS_INMERSIVAS_EXPLORAR_BUTTON = false;
